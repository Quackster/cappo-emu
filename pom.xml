<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cappo</groupId>
    <artifactId>cappo-emu</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>cappo-emu</name>
    <description>Olympus / Capostrike93 Habbo emulator</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>8</maven.compiler.release>
    </properties>

    <dependencies>
        <!-- MySQL JDBC driver (was lib/mysql-connector-java-5.1.32-bin.jar) -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>5.1.32</version>
        </dependency>

        <!-- Netty (was lib/netty-all-5.0.0.Alpha1.jar) -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>5.0.0.Alpha1</version>
        </dependency>

        <!-- Tomcat JDBC connection pool, used by cappo.engine.database.Database -->
        <dependency>
            <groupId>org.apache.tomcat</groupId>
            <artifactId>tomcat-jdbc</artifactId>
            <version>8.5.87</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tomcat</groupId>
            <artifactId>tomcat-juli</artifactId>
            <version>8.5.87</version>
        </dependency>
    </dependencies>

    <build>
        <!-- The original project keeps its sources directly under ./src -->
        <sourceDirectory>src</sourceDirectory>

        <!-- cappo.engine.Server loads Cappo.properties from the classpath root
             via getResourceAsStream("Cappo.properties"). Without this resources
             block, Maven only compiles .java from src/ and never copies
             Cappo.properties to target/classes/, so the server NPEs at boot.
             Keep secrets out of git: Cappo.properties is in .gitignore. -->
        <resources>
            <resource>
                <directory>src</directory>
                <includes>
                    <include>**/*.properties</include>
                </includes>
            </resource>
        </resources>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>8</release>
                    <compilerArgs>
                        <arg>-Xmaxerrs</arg>
                        <arg>2000</arg>
                        <arg>-Xmaxwarns</arg>
                        <arg>100</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>cappo.engine.Server</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>